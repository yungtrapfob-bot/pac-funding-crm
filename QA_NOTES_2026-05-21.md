# QA + Stability Pass (2026-05-21)

## What was checked
- Unauthenticated route behavior for `/dashboard`, `/hot-leads`, `/deals`, and `/deals/:id`.
- Login page shell and action controls rendering.
- Navigation shell consistency (branding, active-state behavior, spacing).
- Table/list overflow behavior on deals table.
- Deal detail and file-link areas reviewed for brittle rendering patterns.

## What was improved
- Polished sidebar brand block and spacing so logo/text alignment is cleaner and less cramped.
- Improved active navigation state logic for nested routes (e.g. deal detail pages now correctly highlight Deals).
- Added signed-in shell top header treatment for clearer app context and a more intentional “internal platform” look.
- Polished login header logo row to avoid visual distortion and improved message handling for magic-link success vs. error.
- Improved deals table horizontal overflow behavior for narrow viewports.
- Added a lightweight automated smoke script for login rendering and unauthenticated redirects on core protected routes.

## Manual testing still needed tomorrow
- Full authenticated shell load (real user session) and role-specific navigation visibility.
- Hot lead create/convert flows end-to-end with real data.
- Deal detail operational save actions, add offer, select offer, and stage move actions.
- Uploaded file open/download links with fresh signed URLs after uploads.
- Logout behavior from all major pages after active use.
- Admin dashboard and admin pipeline content integrity with production-like data.
